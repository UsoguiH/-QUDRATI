# Dev / local
node_modules/
.claude/
*.log
.env

# Local scratch files (dev notes, not part of the app)
figkit.json
WelcomeSVG.txt
قدرات GAME.txt
exam_tab.jpeg

# Figma reference assets (Duolingo IP — local reference only, never publish)
design-ref/

# Internal strategy docs (market research — not for the public repo)
docs/

# Answer-key audit pipeline: keep the scripts + report, ignore regenerable intermediates
tools/methods/in/
tools/methods/out/

# Streak animation dev kit (source/handoff) — the shipped runtime lives in assets/streak/
duolingo-fire-streak/
duolingo-fire-streak.zip

# OS
Thumbs.db
.DS_Store

# Screen recordings used while matching animations (huge, local only)
*.mp4

# Stakeholder pitch deck + its assets — private, never publish
# (the repo is public and Vercel deploys the whole tree)
deck/

# Python bytecode from tools/
tools/__pycache__/

# Mascot: Duolingo reference screenshots (their IP — local reference only, same
# rule as design-ref/) and the edit intermediates that `python tools/mascot_masks.py`
# regenerates. The drawing, the rig, the character sheets (the source the shipped
# states are cut from) and the states themselves stay tracked. The three sheet
# copies at the repo root are the same pixels again — MASCOTS.PNG is the one to
# attach to a prompt, the other two were the raw downloads.
assets/mascot/reference/duolingo/
assets/mascot/edits/
/MASCOTS.PNG
/5 Style Mascots.png
/5 STYLE MASCOTS 2.png
