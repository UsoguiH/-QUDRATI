# Dev / local
node_modules/
.claude/
*.log
.env

# Local scratch files (dev notes, not part of the app)
figkit.json
WelcomeSVG.txt
قدرات GAME.txt
exam_tab.jpeg

# Figma reference assets (Duolingo IP — local reference only, never publish)
design-ref/

# Internal strategy docs (market research — not for the public repo)
docs/

# Answer-key audit pipeline: keep the scripts + report, ignore regenerable intermediates
tools/methods/in/
tools/methods/out/

# OS
Thumbs.db
.DS_Store
